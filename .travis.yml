language: cpp
compiler: gcc
os: linux
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
    env: COMPILER=g++-5
script:
  - mkdir cmake-build && cd cmake-build && cmake ../ && make